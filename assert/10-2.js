// Generated by CoffeeScript 1.8.0
(function() {
  var Todo, addTest, assert, deleteTest, doAsyncTest, testsCompleted, throwsTest, todo;

  assert = require('assert');

  Todo = require('./10.1');

  todo = new Todo();

  testsCompleted = 0;

  deleteTest = function() {
    todo.add('Delete Me');
    assert.equal(todo.getCount(), 1, '1 item should exist');
    todo.deleteAll();
    assert.equal(todo.getCount(), 0, 'No items should exist');
    return testsCompleted++;
  };

  addTest = function() {
    todo.deleteAll();
    todo.add('Added');
    assert.notEqual(todo.getCount(), 0, '1 item should exist');
    return testsCompleted++;
  };

  doAsyncTest = function(cb) {
    return todo.doAsync(function(value) {
      assert.ok(value, 'Callback should be passed true');
      testsCompleted++;
      return cb();
    });
  };

  throwsTest = function(cb) {
    assert.throws(todo.add, /requires/);
    return testsCompleted++;
  };

  deleteTest();

  throwsTest();

  doAsyncTest(function() {
    return console.log('Completed ' + testsCompleted + ' tests');
  });

}).call(this);

//# sourceMappingURL=10-2.js.map
